import {StyleSheet} from "react-native"

const styles = StyleSheet.create({
    row: {
        margin: 5,
        marginTop: 15,
    },
    container: {
        width:"95%",
    }
})

export default styles