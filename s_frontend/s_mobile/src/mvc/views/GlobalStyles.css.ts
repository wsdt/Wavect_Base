import { StyleSheet } from "react-native"

const globalStyles = StyleSheet.create({
    fullSize: {
        height:"100%",
        width:"100%",
    },
    radius: {
        borderRadius: 10,
    },
})

export default globalStyles