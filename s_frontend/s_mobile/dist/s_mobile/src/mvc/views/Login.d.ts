import * as React from "react";
export default class Login extends React.Component {
    state: {
        regForm: {
            btnGroup: {
                selectedIndex: number;
            };
        };
    };
    render(): JSX.Element;
}
