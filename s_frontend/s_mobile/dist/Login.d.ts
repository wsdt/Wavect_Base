import * as React from "react";
export default class Login extends React.Component {
    state: {
        form: {
            isLoading: boolean;
        };
    };
    render(): JSX.Element;
    private submitForm;
}
